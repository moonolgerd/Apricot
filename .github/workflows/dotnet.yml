name: .NET

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: Publish
      run: dotnet publish Apricot/Apricot.WebAssembly/Apricot.csproj -c Release
    - name: GitHub Pages
      uses: moonolgerd/ghaction-github-pages@v2.2.0
      with:
        # Build directory to deploy
        build_dir: bin/Release/net5.0/publish/wwwroot
